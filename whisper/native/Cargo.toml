[package]
name = "cw2"
version = "0.1.0"
edition = "2024"
description = "Native launcher for the isolated CrisperWhisper Local runtime"
license = "MIT"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
