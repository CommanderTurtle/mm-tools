# The studio is loopback-only by default. Deliberately override only for a
# private trusted network and pair it with MM_STUDIO_TOKEN.
V2V_STUDIO_HOST=127.0.0.1
V2V_STUDIO_PORT=8265
# MM_STUDIO_TOKEN=replace-with-a-long-random-value
